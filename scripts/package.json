{
  "name": "@pando/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "seed:dev": "tsx src/seed.ts"
  },
  "dependencies": {
    "@pando/shared": "*",
    "firebase-admin": "^13.0.2"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "vitest": "^2.1.8"
  }
}
